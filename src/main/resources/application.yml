spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/taco_app
    password: Sufyan@123
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: 'true'
  data:
    rest:
      base-path: /api
        
logging:
  level:
    org:
      hibernate:
        type: TRACE
        SQL: DEBUG

#custom config
taco:
  orders:
    pageSize: 10
